'use strict';

// Configuring the Towns module
/*angular.module('towns').run([
  'Menus',
  function (Menus) {
    // Add the towns dropdown item
    Menus.addMenuItem('topbar', {
      title: 'Villes',
      state: 'towns',
      type: 'dropdown'
    });

    // Add the dropdown list item
    Menus.addSubMenuItem('topbar', 'towns', {
      title: 'Liste des villes',
      state: 'towns.list'
    });

    // Add the dropdown create item
    Menus.addSubMenuItem('topbar', 'towns', {
      title: 'Créer une ville',
      state: 'towns.create'
    });

    // Add the dropdown create item
    Menus.addSubMenuItem('topbar', 'towns', {
      title: 'Init elasticsearch',
      state: 'towns.initElasticsearch'
    });

    // Add the dropdown create item
    Menus.addSubMenuItem('topbar', 'towns', {
      title: 'Import',
      state: 'towns.import'
    });
  }
]);
*/