version: '2'
services:

    elasticsearch:
      image: elasticsearch:2.4.0
      volumes:
        - ./.data/elastic:/usr/share/elasticsearch/data
      ports:
        - 9200:9200
        - 9300:9300
